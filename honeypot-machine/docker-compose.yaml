services:
  collector:
    build: .
    container_name: collector
    restart: unless-stopped

    environment:
      MQTT_HOST: ${MQTT_HOST}
      MQTT_PORT: ${MQTT_PORT:-1883}
      
    volumes:
      - .:/app